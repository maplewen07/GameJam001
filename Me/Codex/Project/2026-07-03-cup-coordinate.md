# 2026-07-03 cup coordinate visualization

- Task: use the four blue cups in the photo to establish a 2D coordinate frame and visualize it with Python.
- Coordinate convention: front-left cup is `(0, 0)`, front-right cup is positive x, and the wall-side cups define positive y.
- Implementation: added `cups_coordinate_svg.py`, using only Python standard library to generate a self-contained `cups_coordinate_svg.svg`.
- Verification: ran `python cups_coordinate_svg.py`; parsed the generated SVG with `xml.etree.ElementTree`.
- Update: switched to the second photo, marked the middle cup, and computed its coordinate as approximately `(48.28, 61.06)` in the `100 x 100` frame.
- Update: added `cups_pixel_ratio_distance.py`, which detects blue cup blobs from the PNG and estimates relative near/far distance from pixel height.
- Update: added `cups_3d_tracker.py` for the MP4 workflow. It uses 4 blue floor cups for `x/y`, the right-side static red cup for the image-space `z` axis, tracks the moving red hand cup and white foot cup, and writes `cups_3d_positions.csv` plus `cups_3d_tracker_overlay.mp4`.
- Update: added `red_blue_video_preprocess.py` for `IMG_2485.MOV`. It normalizes brightness, thresholds red/blue in HSV, removes top/right noise regions, writes red/blue mask video, overlay video, and per-frame blob CSV for later marker tracking.
- Update: created `process_2485/` for the `IMG_2485.MOV` preprocessing workflow. `stabilize_space_mask_preprocess.py` stabilizes frames against the first frame, detects the four blue cups, builds the valid space polygon from the cup quadrilateral extended to the video top for `z`, masks red/blue only inside that space, and writes stabilized preview, overlay, mask, calibration JSON, blob CSV, and QA overlay frames.
- Update: exposed CLI tuning parameters in `process_2485/stabilize_space_mask_preprocess.py`: red/blue saturation and value minimums, minimum blob area, and `--tag` for side-by-side tuning outputs without overwriting the default full-video outputs.
- Update: added single-frame tuning mode to `process_2485/stabilize_space_mask_preprocess.py`: `--only-frame N --save-frames` writes stable/mask/overlay JPGs for one video frame so HSV mask parameters can be adjusted quickly.
- Update: rendered full `tune1` video outputs with accepted mask parameters: `--red-s-min 160 --red-v-min 100 --blue-s-min 65 --blue-v-min 15 --blob-min-area 60`. Result: 438 frames, 2290 blobs, with `tune1_*` outputs under `process_2485/`.
- Update: added `process_2485/README.md` summarizing the preprocessing goal, coordinate assumptions, script flow, accepted mask parameters, outputs, calibration file, and current limitations.
- Update: changed `process_2485/stabilize_space_mask_preprocess.py` to the approved preprocessing order. It now detects a fixed red z-cup, writes `XY` homography and relative `Z` axis calibration, adds `space_x/space_y/space_z` columns to the blob CSV, draws XYZ axes in the overlay, and writes a ground-plane `topdown_red_blue_mask` preview. Rendered full `tune1_3d_*` outputs with the accepted mask parameters.
- Update: corrected the `tune1_3d` crop polygon to match the user-marked red-box intent: front two blue cups define the bottom edge, and the fixed red z-cup direction defines the side edges. The rear blue cups now only affect `XY` homography, not the crop boundary. Re-rendered full `tune1_3d_*` outputs; result: 438 frames, 2741 blobs.
- Update: added `process_2485/stable_01.md` as the stable preprocessing record for `IMG_2485.MOV`, covering the approved flow, coordinate convention, mask parameters, command, outputs, stats, and known limits.
- Update: added `process_2485/track_hand_foot_stable01.py` to post-process `tune1_3d_space_red_blue_blobs.csv`. It excludes the fixed red z-cup, classifies remaining red markers into foot/hand with a simple y/area heuristic, assumes z-cup ground height `1.0`, and writes `tune1_3d_hand_foot_positions.csv`. Result: 438 frames, 210 ok hand+foot frames, 205 foot-only frames, 23 missing-red frames.
- Update: changed `track_hand_foot_stable01.py` so frames with no usable red marker copy the previous frame result and mark `status=filled_from_previous`. Rebuilt `tune1_3d_hand_foot_positions.csv`; result: 210 ok frames, 205 foot-only frames, 23 filled-from-previous frames, 0 missing-red frames.
- Update: changed `track_hand_foot_stable01.py` so empty hand/space fields also inherit the previous value, then `foot_space_*` and `hand_space_*` are smoothed with a 30-frame triangular weighted window and distances are recomputed. Rebuilt `tune1_3d_hand_foot_positions.csv`; result: 438 rows, 0 empty 2D/3D distance fields.
- Update: processed `F:\Github\GameJam\IMG_2487.MOV` in new `process_2487/` with the stable_01 workflow. Copied the preprocessing and hand/foot scripts, set the default video path to `IMG_2487.MOV`, rendered `tune1_3d_*` outputs, generated `tune1_3d_hand_foot_positions.csv`, and wrote `process_2487/stable_01.md`. Result: 639 frames, 4439 blobs, 631 ok hand+foot frames, 8 foot-only frames, 0 empty 2D/3D distance fields.
